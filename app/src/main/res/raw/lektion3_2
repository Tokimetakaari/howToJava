3.2 Arithmetische Operatoren:

"+" wird zur Addition verwendet --> Bsp.: int lösung = a + b;
"-" wird zur Subtraktion verwendet --> Bsp.: int lösung = a - b;
"*" wird zur Multiplikation verwendet --> Bsp.: int lösung = a * b;
"/" wird zur Division verwendet --> Bsp.: int lösung = a / b;
"%" ist ein Modulo-Operator, welcher zum Rechnen mit Rest verwendet wird --> Bsp.: int lösung = a % b;